#include <iostream>
#include <string>
#include "wall_stream.hpp"
#include "screen.hpp"

using namespace std;

int main(){
    //  Variables to store data for screen

    char w[MAXBUF], h[MAXBUF], x[MAXBUF], y[MAXBUF], p[MAXBUF], id[MAXBUF];
    
    create_config_default();
    //  Open Config.conf

    //  Count the Id's in .conf

    //  Create screen

    //  Dump Screen info
    cout << "Goodbye C, Hello C++\n";
    
    return 0;
}